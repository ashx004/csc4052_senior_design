What is pygame?

Pygame - a python library
	* a collection of python modules
	* cross platform
	* video, audio, mouse, keyboard
	
	
by Pete Shinners (c programmer)

SDL - cross platform library to access your system's hardware
	* "you're running c code in python"
	* Simple multimedia layer
	
Why - 
	* simple 2d games
	
Learn github?

import library
import pygame
import sys

initialize pygame
pygame.init()

speed [2, 2]
black = 0, 0, 0


define screen size
screen = pygame.display.set_mode(size)

ball = pygame.image.load("intro_ball.gif")
ballrect = ball.get_rect()

event loops?

learn for loops

get user input

screen.fill(black)
first place the ball on the screen
screen.blit(ball, ballrect)
then an update function
pygame.display.flip()

november 8th task automation in python