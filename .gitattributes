# Regenerated by scripts/copy-pdf-worker.mjs on every install. Mark it binary so
# git never rewrites its line endings, which otherwise leaves the tree dirty
# after a fresh `npm ci` on Windows.
public/pdf.worker.min.mjs -text
